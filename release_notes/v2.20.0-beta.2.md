## 🪟 Dual-Axis Venetian Blind Support — Beta 2 (closes #33)

Beta.2 adds a **Target Tilt sensor**, fixes the post-tilt reconciliation loop
that caused the integration to keep re-sending tilt commands after the cover
settled, and hardens the internal architecture behind a `CoverTypePolicy` layer
so that cover-type-specific behavior (geometry, entity selectors, glare zones)
is cleanly isolated. All beta.1 features are included.

> ### 🙏 Looking for Beta Testers
>
> Beta.2 is calling in the volunteers from [#33]: **fnep**, **x4N70pHyLL**,
> **geryyyyyy** (Somfy IO), **MetalMinotaur** (KNX), and **jkuchar** (Shelly 2PM).
> Install beta.2 and report back on issue #33 — especially whether the tilt
> commands settle cleanly without the repeated-send loop from beta.1.

### ✨ New in Beta 2

- **Target Tilt sensor (#33):** A new diagnostic sensor exposes the tilt angle the
  engine is targeting (in degrees), making it easy to verify the slat-angle
  calculation without downloading a full diagnostics bundle. Visible in the HA
  entity list alongside the existing Target Position sensor.

- **Post-tilt reconciliation loop fix:** After `set_cover_tilt_position` fired,
  the coordinator was treating each subsequent `current_tilt_position` state
  change as a new deviation and re-sending the tilt command. The fix makes the
  reconciliation loop aware of in-flight tilt commands and skips re-sends while
  the motor is settling.

- **`CoverTypePolicy` refactor (internal):** Venetian-specific geometry schema,
  entity-selector domains, and glare-zone support are now gated through a
  `CoverTypePolicy` interface. This is a pure internal change — no user-visible
  behavior is affected — but it eliminates several `if sensor_type == "cover_venetian"`
  branches that had been accumulating in the coordinator and config flow.

### 🧪 Testing

- 2735 tests passing (includes all beta.1 test additions).

[#33]: https://github.com/jrhubott/adaptive-cover-pro/issues/33
